/**
 * JUnit test suite for the SocialNetwork application.
 *
 * <p>Responsibilities:
 * <ul>
 *   <li>Verify correct behaviour of {@code addMember} (nominal cases and boundary cases).</li>
 *   <li>Verify correct behaviour of {@code addItemBook} (nominal cases and boundary cases).</li>
 *   <li>Provide regression coverage so that new features do not break existing ones.</li>
 * </ul>
 */
package tests;
